# YOLO model weights (auto-downloaded on first run if missing)
# After training: YOLO_MODEL=runs/detect/person_yolo11n/weights/best.pt
YOLO_MODEL=yolo11n.pt

# Optional - Kaggle API for CrowdHuman download (scripts/download_dataset.ps1)
# New tokens (KGAT_...): save to .kaggle/access_token or set:
# KAGGLE_API_TOKEN=
# Legacy credentials only:
# KAGGLE_USERNAME=
# KAGGLE_KEY=

# Tracker config: bytetrack.yaml or botsort.yaml
TRACKER=bytetrack.yaml

# Video source: integer webcam index, file path, or rtsp URL
VIDEO_SOURCE=0

# Detection confidence threshold (0.0 - 1.0)
CONFIDENCE=0.5

# Enable face recognition layer (requires: pip install -r requirements-face.txt)
FACE_RECOGNITION_ENABLED=true

# Folder with subfolders per person: data/known_faces/Alice/photo.jpg
KNOWN_FACES_DIR=data/known_faces

# Face match threshold (lower = stricter)
FACE_MATCH_THRESHOLD=0.4

# DeepFace model backend
FACE_MODEL=Facenet512

# Web server
HOST=127.0.0.1
PORT=8765
